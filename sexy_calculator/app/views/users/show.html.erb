<p>
<strong>First Name:</strong>
<%= @user.first_name %>

</p>
<p>
<strong>Last Name:</strong>
<%= @user.last_name %>

</p>
<p>
<strong>Email:</strong>
<%= @user.email %>
</p>
<p>
  <strong>Orders:</strong>
  <ul>
  <% @user.orders.each do |order| %>
    <li>
      <%= link_to "Fiber Length "+order.fiber_length.to_s+" Copper Length "+order.copper_length.to_s+" Created at "+order.updated_at.to_s, edit_order_path(order) %> <%#= link_to'Email Order', (order) %>
    </li>

  <% end %>
  </ul>
</p>

